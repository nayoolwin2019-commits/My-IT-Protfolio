-- Create a table for Employees
CREATE TABLE Employees (
    ID int,
    Name varchar(255),
    Position varchar(255)
);

-- Insert your info
INSERT INTO Employees (ID, Name, Position)
VALUES (1, 'Iloveyou', 'Chit Su Thansin');

-- Show the data
SELECT * FROM Employees;

